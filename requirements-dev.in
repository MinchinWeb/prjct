-r requirements.txt
# ablog >= 0.8
# sphinx >= 1.3
releases
green >=1.9.4
pip-tools
minchin.releaser
check-manifest

# manually bump min version for security
lxml >= 4.6.5

# 'cloud_sptheme >=1.8',
# 'Babel >=1.3,!=2.0',  # 2.0 breaks on Windows
# 'coverage',
# 'isort',
# 'pydocstyle',
# 'pycodestyle',
